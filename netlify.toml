[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# SPA fallback: any path that isn't a real file resolves to index.html, so
# client-side routing (once added) and hard-refreshing a deep link both work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
